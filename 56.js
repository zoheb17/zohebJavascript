// ASychronous (non blocking)


// these are delegated  to bowser or node.js api and run in the back ground , allowing js to move on



// COMMON ASYNC OPERATION


// type                                         example
                                          
// timers                                        setTimeout().setInterval

// promises                                      promice.resolve(). promise.reject(), .then(), .catch

// asy function                                  async function getData () { await fetch (....)}

// fatch api                                    fetch("https://api.example.com")

// network request                              XMLHttRequest, axios.get()

// file system (node.js)                        fd.readFile(), fs writerFile() (async version)

// database  q ueries                          mongoDB, MySQL queries etc                                

// event listner                               button.addeventListener("click",...)

// animation frames                            requestAnimationFrame()

// web workers                                 backgroung thread for heavy computation
